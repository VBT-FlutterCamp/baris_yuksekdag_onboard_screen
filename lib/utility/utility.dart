import 'package:flutter/material.dart';

class Utilities {}

class emptySpace extends SizedBox {
  const emptySpace.xxsHeight({Key? key}) : super(key: key, height: 10);
  const emptySpace.xsHeight({Key? key}) : super(key: key, height: 20);
  const emptySpace.sHeight({Key? key}) : super(key: key, height: 30);
  const emptySpace.mHeight({Key? key}) : super(key: key, height: 40);
  const emptySpace.xlHeight({Key? key}) : super(key: key, height: 50);
  const emptySpace.xxlHeight({Key? key}) : super(key: key, height: 60);
  const emptySpace.xxsWidth({Key? key}) : super(key: key, width: 10);
  const emptySpace.xsWidth({Key? key}) : super(key: key, width: 20);
  const emptySpace.sWidth({Key? key}) : super(key: key, width: 30);
  const emptySpace.mWidth({Key? key}) : super(key: key, width: 40);
  const emptySpace.xlWidth({Key? key}) : super(key: key, width: 50);
  const emptySpace.xxlWidth({Key? key}) : super(key: key, width: 60);
}
